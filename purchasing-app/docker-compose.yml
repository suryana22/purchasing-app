version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: purchasing_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Suryana@130221
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - purchasing_network

  master-data-service:
    build: ../master-data-service
    container_name: master-data-service
    restart: always
    ports:
      - "4001:4001"
    volumes:
      - ../master-data-service:/app
      - /app/node_modules
    command: npm run dev
    environment:
      - PORT=4001
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - DB_PORT=${DB_PORT}
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - postgres
    networks:
      - purchasing_network

  purchasing-service:
    build: ../purchasing-service
    container_name: purchasing-service
    restart: always
    ports:
      - "4002:4002"
    volumes:
      - ../purchasing-service:/app
      - /app/node_modules
    command: npm run dev
    environment:
      - PORT=4002
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - DB_PORT=${DB_PORT}
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - postgres
    networks:
      - purchasing_network

  frontend-app:
    build: ./frontend
    container_name: frontend-app
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    env_file: .env
    environment:
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - master-data-service
      - purchasing-service
    networks:
      - purchasing_network

  cloudflare-tunnel:
    image: cloudflare/cloudflared:latest
    container_name: cloudflare-tunnel
    restart: always
    command: tunnel --no-autoupdate run --token eyJhIjoiY2Q0MzI2YmQ4NTg4YjllYzNlMmVmY2RkNzFkZGY0NGMiLCJ0IjoiNDc3YThiMzQtYWExZi00OTIwLWE0ZTEtMDc2YTBlNzMzMmUxIiwicyI6IlpHSTRNMlJrTkdFdFpqWXhPQzAwWWpRMUxXSXdORGN0T0dGbE5XVTRNekpqWmpFNSJ9
    networks:
      - purchasing_network

networks:
  purchasing_network:
    driver: bridge

volumes:
  postgres_data:
